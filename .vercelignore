# Vercel preview deploys include api/ and server/ so branch API code is
# packaged with the preview. Production custom hosts still proxy to the
# production API through vercel.json.
*.sql
*.bundle
migrations/
nixpacks.toml
railway.json
