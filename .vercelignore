# Vercel Preview runs the branch API against Proof, so api/ and server/ must ship.
# Keep database/source-environment artifacts out of the deployment bundle.
*.sql
migrations/
nixpacks.toml
railway.json
