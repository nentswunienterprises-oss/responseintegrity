name: Preview API Bundle Diagnostic

on:
  push:
    branches:
      - feat/demand-production-gateway
    paths:
      - '.github/workflows/preview-api-bundle-diagnostic.yml'
  workflow_dispatch:

jobs:
  bundle-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci --ignore-scripts

      - name: Bundle preview API
        run: |
          npx esbuild api/index.ts \
            --bundle \
            --platform=node \
            --format=esm \
            --packages=external \
            --target=node24 \
            --outfile=/tmp/ri-preview-api.mjs

      - name: Import bundled preview API
        env:
          NODE_ENV: production
          VERCEL_ENV: preview
          SUPABASE_URL: https://example.supabase.co
          SUPABASE_ANON_KEY: diagnostic-anon-key
          DATABASE_URL: postgresql://user:pass@localhost:5432/db
          SESSION_SECRET: diagnostic-session-secret
        run: |
          node --input-type=module <<'NODE'
          try {
            await import('file:///tmp/ri-preview-api.mjs');
            console.log('PREVIEW_API_BUNDLE_IMPORT=PASS');
          } catch (error) {
            console.error('PREVIEW_API_BUNDLE_IMPORT=FAIL');
            console.error(error?.stack || error);
            process.exit(1);
          }
          NODE
